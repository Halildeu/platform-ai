# meeting-ai-service runtime dependencies (skeleton — mock backend).
# Real LLM SDKs (anthropic / openai / ollama client) are intentionally NOT pinned
# here: those backends are stubs pending the ADR-0030 Option A/B decision.
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
structlog==24.4.0
prometheus-client==0.21.0
httpx==0.27.2
cryptography==46.0.7
redis==5.1.1
# gitops#3444: Turkish morphology for citation grounding recall (Zemberek port).
# Word-level _parse only — no NLTK data download; see app/services/morphology.py.
zeyrek==0.1.3
